[build]
  command = "cd frontend && npm install --legacy-peer-deps && npm install react-scripts --save-dev && npm run build"
  publish = "frontend/build"
  functions = "functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

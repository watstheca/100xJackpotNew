[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "frontend/build"  # Changed to include frontend path
  functions = "netlify/functions"  # Updated to match the full path

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200